# Global Assumptions

<!-- 
  全域假設清單。
  所有 theorem / lemma 引用的假設都必須在此文件中有對應條目。
  禁止在 proof 中偷偷引入此處未列出的假設。
-->

## Active Assumptions

### A001 — Domain regularity

$$
\Omega \text{ is a bounded Lipschitz domain in } \mathbb{R}^d
$$

Used by: (list theorem/lemma IDs)

---

### A002 — Solution regularity

$$
u \in H^k(\Omega)
$$

Used by: (list theorem/lemma IDs)

---

### A003 — Mapping smoothness

The coordinate mapping $x(\xi)$ is smooth and satisfies $J > 0$.

Used by: (list theorem/lemma IDs)

---

### A004 — SBP property

The discrete derivative operator $D$ satisfies the SBP property:

$$
M D + (M D)^T = E
$$

where $E$ is the boundary quadrature matrix.

Used by: (list theorem/lemma IDs)

---

<!-- 新增假設請使用遞增編號 A005, A006, ... -->

## Retired Assumptions

<!-- 曾經使用但已被替換或移除的假設 -->

(none yet)
